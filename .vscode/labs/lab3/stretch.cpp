#include <iostream>
#include <cmath>
using namespace std;
int main(){
	string letter;
  string sentance;
	getline(cin, sentance);
  getline(cin, letter);
  int i = 0;
  int foundTimes = 0;
  
  for(int = 0; i < target.size(); i++){
    if(target.at(i) == letter.at(0)){
      foundTimes++;
    }
  }
  
  cout << foundTimes << endl;
	
}
